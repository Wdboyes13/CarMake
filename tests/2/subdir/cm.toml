[package]
name = "test2sub"
type = "exec"
installdir = "/usr/local/bin/"

[build]
compiler = "clang"
cflags = "-O2 -Wall"
includes = ["gmp"]
libdirs = ["gmp"]
libs = ["gmp"]
sources = ["main.c", "file2.c"]